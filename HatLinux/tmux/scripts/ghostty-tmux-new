#!/bin/sh
# Compatibility shim — delegates to the canonical wrapper.
# This file exists during the migration window (DG-3 steps 1-7) so that
# users with a stale ~/.local/bin/ghostty-tmux-new symlink still work.
# It will be removed at step 8 of the sequencing plan.
exec ~/.local/bin/ghostty-multiplexer-new "$@"